<UserControl x:Class="Thermostat.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Thermostat.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Slider HorizontalAlignment="Stretch" Margin="10,30,10,0" VerticalAlignment="Top" Height="30" Minimum="50" Maximum="90" SmallChange="1" TickFrequency="5" TickPlacement="BottomRight" Value="70" />
        
        <TextBlock HorizontalAlignment="Center" Text="65°" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="72" TextAlignment="Center"/>
        <Button Content="History" Command="{Binding OpenHistoryCommand}" HorizontalAlignment="Center" Margin="10,106,10,0" VerticalAlignment="Center" Width="100"/>
        
        <ComboBox HorizontalAlignment="Left" Margin="10" VerticalAlignment="Bottom" Width="120">
            <ComboBoxItem IsSelected="True">HeatOnly</ComboBoxItem>
        </ComboBox>
        <Button Content="Settings" Command="{Binding OpenSettingsCommand}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" Width="60" Height="60"/>

    </Grid>
</UserControl>
